---
  - name: Add users 
    user:
      name: "{{ item }}"
      password: ""
      update_password: on_create
      state: present
    with_items:
      - Alan
      - Jesse
      - Agatha
    tags:
      - AddUser

  - name: force user change password
    shell: chage -d 0 "{{ item }}"
    with_items:
      - Alan
      - Jesse
      - Agatha
    tags:
      - AddUser
