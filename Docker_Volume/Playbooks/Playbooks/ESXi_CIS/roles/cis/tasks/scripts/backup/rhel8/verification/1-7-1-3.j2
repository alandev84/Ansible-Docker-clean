#!/bin/sh

grep -E '^\s*SELINUXTYPE=(targeted|mls)\b' /etc/selinux/config \
&& \
sestatus | grep -E '^Loaded\s+policy\s+name:\s+(targeted|mls)\b\s*$' \
|| \
(echo 'SELinux is not targeted or mls.' ; exit 1)
